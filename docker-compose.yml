services:
  elvex-api:
    image: elvex-api:latest
    
    build:
      context: .
      dockerfile: Dockerfile

    container_name: elvex-api

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./outputs:/app/outputs

    restart: unless-stopped