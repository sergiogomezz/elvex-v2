paper prep

To set up:
    1. Create an .env file and add credentials (OPENAI_API_KEY = your-key, OPENAI_MODEL = )
    2. Be sure to use the model you want on src/elvex/config/settings.py
    3. Install package uv pip install -e . This is for letting the code know that src/elvex is the library to import